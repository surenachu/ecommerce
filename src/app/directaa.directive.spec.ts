import { DirectaaDirective } from './directaa.directive';

describe('DirectaaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectaaDirective();
    expect(directive).toBeTruthy();
  });
});
